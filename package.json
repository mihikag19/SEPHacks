{
  "name": "drift-sentinel",
  "version": "0.1.0",
  "private": true,
  "description": "GxP-aware cloud configuration drift detection for biotech",
  "workspaces": [
    "api",
    "dashboard"
  ],
  "scripts": {
    "build": "npm run build --workspace=api",
    "dev": "npm run dev --workspace=api",
    "lint": "npm run lint --workspace=api",
    "typecheck": "npm run typecheck --workspace=api"
  }
}
